class Weather {
  final int id;
  final String title, weather;
  final double temp, windspeed;

  Weather({
    required this.id,
    required this.title,
    required this.temp,
    required this.windspeed,
    required this.weather,
  });
}